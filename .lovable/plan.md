Remove the "View full profile →" link from the bio dialog in `src/pages/TeamDetails.tsx`. The team member name in the grid will still link to `/team/:slug` for anyone who wants the dedicated profile page.